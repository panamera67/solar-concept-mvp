{
  "name": "solar-concept",
  "version": "0.1.0",
  "private": true,
  "description": "Solar Concept - Système de correction automatique Enterprise monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces",
    "test": "npm run test --workspaces"
  }
}
